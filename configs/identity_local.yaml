# Identity baseline for the local sample set.
original_root: ../../Test
attack: identity
save_attacked_dir: null
models_dir: ../weights
save_json: ../experiments/results_identity_local.json
classifiers:
  - vit_b_16
  - densenet121_dct
dct_log_scale: true
weights:
  vit_b_16: 1.0
  densenet121_dct: 1.0
aggregate: sum
