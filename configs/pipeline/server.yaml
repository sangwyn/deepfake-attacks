schema_version: 1

# The Git checkout contains code, agent policy, and compact experiment records.
project_root: /home/aiattacks/oleg/aadd-attack-pipeline

dataset:
  celeb_a_root: /home/aiattacks/dataset/celebA
  official_aadd_root: null
  read_only: true
  classes:
    TRAIN_REAL: {path: TRAIN/TRAIN_REAL, label: 0, expected_count: 1500}
    TRAIN_FAKE: {path: TRAIN/TRAIN_FAKE, label: 1, expected_count: 1500}
    TEST_REAL: {path: TEST/TEST_REAL, label: 0, expected_count: 100}
    TEST_FAKE: {path: TEST/TEST_FAKE, label: 1, expected_count: 100}

assets:
  # Populate this directory without committing binaries. Preflight verifies hashes.
  weights_root: /home/aiattacks/oleg/aadd-attack-assets/weights
  models:
    vit_b_16:
      filename: vit_b_16.pth
      sha256: 5e9677d88a7af10791001796eb43d0d060fada3758369814d6d7832934758d81
    densenet121_dct:
      filename: densenet121_dct.pth
      sha256: 5bbaf5c5c0e296d5e819a0b401198c73ad69c6bbc8f372579de5ee5c11d5e643
  lpips:
    # Existing shared, read-only Torch cache; no automatic download is permitted.
    alexnet_backbone_path: /home/aiattacks/.cache/torch/hub/checkpoints/alexnet-owt-7be5be79.pth
    alexnet_backbone_sha256: 7be5be791159472b1fbf3c69796f7cb30dca7ad8466c2df70058c37116cdee02
    # This calibration file is resolved inside the pinned lpips package.
    calibration_relative_path: weights/v0.1/alex.pth
    calibration_sha256: df73285e35b22355a2df87cdb6b70b343713b667eddbda73e1977e0c860835c0

runtime:
  # Generated images and verbose logs are kept away from both code and dataset.
  runs_root: /home/aiattacks/oleg/aadd-attack-runs
  tracking_root: tracking

environment:
  lock_file: environment.lock.json
  python: "3.12.3"
  torch: 2.3.0+cu121
  torchvision: 0.18.0+cu121
  cuda_wheel_runtime: "12.1"
  minimum_driver: "525.60"

opencode:
  binary: /home/aiattacks/.opencode/bin/opencode
  version: 1.18.26
  model: naapi/gpt-5.6-terra

gpu_queue:
  state_dir: .gpuq
  max_running: 1
  minimum_free_memory_mib: 40000
  idle_samples: 3
  sample_interval_seconds: 20
  launch_grace_seconds: 30
